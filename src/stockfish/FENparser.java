package stockfish;

import gui.ApplicationConstants;
import gui.Square;

public class FENparser {
	/*
	Square[][] squareList = new Square[8][8];
	
	public Square[][] generateSqueareList(String FEN){
		
		String[] splittedFEN = FEN.split("/");
		
		for (int row = 0; row <= ApplicationConstants.NUMROWS; row++) {
			for (int column = 0; column <= ApplicationConstants.NUMCOLUMNS; column++) {
				String[] columnSymbol = splittedFEN[row].split("");
				for (int i = 0; i < columnSymbol.length; i++) {
					if(columnSymbol[i].matches("[rnbqkpRNBQKP]")){
						if(columnSymbol[i] == ApplicationConstants.SYMBOL_BLACK_BISHOP){
							squareList[column][row].add();
						}
						
					}
				}
			}	
		}
		
		return squareList;
	}
*/
}
